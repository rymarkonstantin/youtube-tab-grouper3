import "./styles.css";

export function initializeOptions(): void {
  // Later bundles replace this shell with the persistent category editor.
}

if (typeof document !== "undefined") {
  initializeOptions();
}

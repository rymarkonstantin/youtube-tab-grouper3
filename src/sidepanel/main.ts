import "./styles.css";

export function initializeSidePanel(): void {
  // Later bundles replace this shell with the explicit grouping workflow.
}

if (typeof document !== "undefined") {
  initializeSidePanel();
}
